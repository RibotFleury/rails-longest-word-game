<div><h1>New game</h1></div>

<div>
<% @letters.each do |letter| %>
  <span class="background-span"><%= letter %></span>
<% end %>

</div>

<div>
<form action="/score" method="get" data-turbo="false">
   <div class="form-group mb-2 d-flex justify-content-start"> <!-- ... -->
  <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
  <%= hidden_field_tag :letters, @letters %>

  <input type="text" name="word" value="<%= params[:word] %>" placeholder="Give me a word" class="form-control">
  <input type="submit" value="" class="btn btn-primary">
  </div>
</form>

</div>
